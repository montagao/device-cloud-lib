sudo: false
language: c
compiler:
  - clang
  - gcc
os:
  - linux
env:
  matrix:
    - BUILD_TYPE=Debug
    - BUILD_TYPE=Release

addons:
  apt:
    packages:
      - cmake
      - libarchive-dev
      - libjansson-dev
      - libjson-c-dev
      - libcurl4-openssl-dev

install:
  - mkdir build
  - cd build
  - ../build.sh

before_script:
  - cmake --version

script:
  - make
  - make check
  - make package

